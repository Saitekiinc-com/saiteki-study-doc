name: Auto Label by Checkbox

on:
  issues:
    types: [opened, edited, labeled, reopened, unlabeled]

jobs:
  label-manager:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    if: contains(github.event.issue.labels.*.name, 'book-search-request')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Manage Labels
        uses: actions/github-script@v7
        with:
          script: |
            const { syncLabels } = require('./scripts/sync-labels.js');
            await syncLabels({ github, context, core });
